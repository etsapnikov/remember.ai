<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Возврат обязан приходить в пределах 2 минут от плана (§8), иначе метрика
         меряет прошивку, а не продукт. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- Приложение ставит само себя: обновление dogfood-сборки без диалога
         (Android 12+, USER_ACTION_NOT_REQUIRED). Ставится только APK с нашей
         подписью — проверка в SelfUpdater. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <uses-feature android:name="android.hardware.microphone" android:required="true" />

    <application
        android:name=".PrinyalApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.Prinyal">

        <!-- Между желанием и записью нет ни одного экрана: иконка открывает
             приложение сразу в запись (PRD §F-1).
             adjustResize, не adjustPan: при правке транскрипта окно должно
             сжиматься, иначе клавиатура уводит вверх и шапку, и статус-бар. -->
        <activity
            android:name=".capture.CaptureActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:excludeFromRecents="true"
            android:taskAffinity=".capture"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Prinyal.Capture">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.MainActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Prinyal" />

        <!-- Виджет 1×1 — тот же жест, что иконка. -->
        <receiver
            android:name=".capture.CaptureWidget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/capture_widget_info" />
        </receiver>

        <!-- Плитка Quick Settings — продукт живёт в шторке. -->
        <service
            android:name=".capture.CaptureTileService"
            android:exported="true"
            android:icon="@drawable/ic_tile"
            android:label="@string/tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <receiver
            android:name=".returns.ReturnAlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".returns.ReturnActionReceiver"
            android:exported="false" />

        <receiver
            android:name=".returns.DayAskReceiver"
            android:exported="false" />

        <receiver
            android:name=".returns.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- exported, потому что команду шлёт `adb shell am broadcast` от uid shell.
             Замок не здесь, а в проверке подписи APK. -->
        <receiver
            android:name=".update.UpdateReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="ai.prinim.prinyal.action.SELF_UPDATE" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".update.UpdateStatusReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="ai.prinim.prinyal.action.SELF_UPDATE_STATUS" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
